#!/bin/bash
# Lista pacchetti da installare - packages.conf

# Pacchetti essenziali di sistema
SYSTEM_PACKAGES=(
    "base-devel"
    "sudo"
    "curl"
    "wget"
    "git"
    "nano"
    "htop"
    "rsync"
    "python"
    "python-pip"
    "python-poetry"
    "python-pipx"
    "cronie"
    "qt5-base"
    "qt5-tools"
    "jq"
    "unzip"
    "zip"
    "github-cli"
)

# Pacchetti Docker
DOCKER_PACKAGES=(
    "docker"
    "docker-compose"
)

# Pacchetti di sicurezza
SECURITY_PACKAGES=(
    "fail2ban"
    "ufw"
)

# Pacchetti di ottimizzazione
OPTIMIZATION_PACKAGES=(
    "reflector"
)

# Array completo di tutti i pacchetti
ALL_PACKAGES=(
    "${SYSTEM_PACKAGES[@]}"
    "${DOCKER_PACKAGES[@]}"
    "${SECURITY_PACKAGES[@]}"
    "${OPTIMIZATION_PACKAGES[@]}"
)